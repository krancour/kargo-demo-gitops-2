configVersion: v1alpha1
branchConfigs:
- pattern: \d+/stage/(\w+)
  appConfigs:
    kargo-demo:
      configManagement:
        path: charts/kargo-demo
        helm:
          valueFiles:
          - charts/kargo-demo/stages/${1}/values.yaml
  preservedPaths:
  - README.md
