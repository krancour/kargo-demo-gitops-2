apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-20
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-20
  template:
    metadata:
      labels:
        app: app-20
    spec:
      containers:
      - name: nginx 
        image: public.ecr.aws/nginx/nginx:placeholder
