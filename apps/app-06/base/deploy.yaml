apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-06
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-06
  template:
    metadata:
      labels:
        app: app-06
    spec:
      containers:
      - name: nginx 
        image: public.ecr.aws/nginx/nginx:placeholder
