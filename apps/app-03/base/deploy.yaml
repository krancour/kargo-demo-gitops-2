apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-03
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-03
  template:
    metadata:
      labels:
        app: app-03
    spec:
      containers:
      - name: nginx 
        image: public.ecr.aws/nginx/nginx:placeholder
