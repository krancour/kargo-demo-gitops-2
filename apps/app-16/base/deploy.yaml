apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-16
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-16
  template:
    metadata:
      labels:
        app: app-16
    spec:
      containers:
      - name: nginx 
        image: public.ecr.aws/nginx/nginx:placeholder
