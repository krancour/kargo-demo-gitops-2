apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-08
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-08
  template:
    metadata:
      labels:
        app: app-08
    spec:
      containers:
      - name: nginx 
        image: public.ecr.aws/nginx/nginx:placeholder
