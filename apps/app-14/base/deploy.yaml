apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-14
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-14
  template:
    metadata:
      labels:
        app: app-14
    spec:
      containers:
      - name: nginx 
        image: public.ecr.aws/nginx/nginx:placeholder
