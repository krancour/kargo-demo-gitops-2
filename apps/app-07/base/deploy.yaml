apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-07
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-07
  template:
    metadata:
      labels:
        app: app-07
    spec:
      containers:
      - name: nginx 
        image: public.ecr.aws/nginx/nginx:placeholder
